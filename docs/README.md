# 🪄 Math formulas
## Area
- Circle: S = πR²
- Rectangle: S = ab
- Square: S = a²
- Triangle: S = ah/2
## Perimeter
- Circle: P = 2πR
- Rectangle: P = 2a + 2b
- Square: P = 4a
- Triangle: S = a + b + c





# ⚙️ Общее описание решения

Проект представляет собой набор функций для вычисления **площади** и **периметра** основных геометрических фигур:  
- **круга**,  
- **прямоугольника**,  
- **треугольника**,  
- **квадрата**.  

Каждая функция реализует простую формулу, основанную на стандартных геометрических зависимостях.  

---

## 🧩 Описание функций с примерами вызова

### 🔵 Круг

#### `area(r)`
> Вычисляет площадь круга.

**Параметры:**
- 'r' — радиус круга.

**Возвращает:**  
Площадь круга по формуле *π*r².

**Пример:**
```python
area(5) = 78.53981
```


#### `perimeter(r)`

**Параметры:**
- 'r' — радиус круга.

**Возвращает:**  
Площадь круга по формуле 2*π*r².

**Пример:**
```python
    area(5) = 31.415926
```

### 🟪 Прямоугольник

#### `area(a, b)`
> Вычисляет площадь прямоугольника.

**Параметры:**
- 'a' - длина какой-то из сторон
- 'b' - длина смежной с ней стороны

**Возвращает:**  
Площадь прямоугольника по формуле a * b.

**Пример:**
```python
area(3, 5) = 15
```


#### `perimeter(a, b)`

**Параметры:**
- 'a' - длина какой-то из сторон
- 'b' - длина смежной с ней стороны

**Возвращает:**  
Площадь прямоугольник по формуле 2*(a + b).

**Пример:*
```python
area(3, 5) = 16
```

### 🔺 Треугольник

#### `area(a, h)`
> Вычисляет площадь треугольника.

**Параметры:**
- 'a' — длина основания
- 'h' — длина, высоты, проведённой к этому основанию

**Возвращает:**  
Площадь треугольник по формуле a * h / 2.

**Пример:**
```python
area(4, 5) = 10
```

#### `perimeter(a, b, c)`

**Параметры:**
- 'a, b, c' — длины сторон


**Возвращает:**  
Площадь треугольника по формуле a + b + c.

```python
area(3, 4 ,5) = 12
```

### 🟥 Квадрат

#### `area(a)`
> Вычисляет площадь квадрата.

**Параметры:**
- 'a' — длина сторон

**Возвращает:**  
Площадь квадрата по формуле a * а.

**Пример:**

```python
area(4) = 16
```


#### `perimeter(a)`

**Параметры:**
- 'a' — длина сторон


**Возвращает:**  
Площадь квадрата по формуле 4*а.

**Пример:**
```python
    area(5) = 20
```
---

## 🐾 История изменения проекта
### 1️⃣ Первое изменение:
Добавлены файлы с функциями для вычисления площади и периметра круга и квадрата.
##### Хэш изменения первого изменения:
> 8ba9aeb3cea847b63a91ac378a2a6db758682460
### 2️⃣ Второе изменение:
Добавлен файл с документацией
##### Хэш второго изменения:
> d078c8d9ee6155f3cb0e577d28d337b791de28e2
### 3️⃣ Третье изменение:
Добавлен файл с функциями для вычисления площади и периметра прямоугольника.
##### Хэш третьего изменения:
> 48f26c2d0db0ea429c4179b77f65f6f94641209e
### 4️⃣ Четвёртое изменение:
Добавлен файл с функциями для вычисления площади и периметра треугольника. Исправлена ошибка в подсчёте периметра прямоугольника.
##### Хэш четвёртого изменения:
>504786c9f422a4dcecfd25c884b9013de3b66a8%
